{
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "test": "lerna run test --since ",
    "test-common": "lerna run test --scope=@gskTest/common",
    "test-deps": "lerna run test --scope={@gskTest/server,@gskTest/front}",
    "new-version": "lerna version --conventional-commits --yes --create-release github ",
    "diff": "lerna diff"
  },
  "name": "yarn-workspaces",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "devDependencies": {
    "lerna": "^3.22.1"
  }
}
