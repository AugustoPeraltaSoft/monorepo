{
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "test": "lerna run test --since ",
    "test-common": "lerna run test --scope=@gskTest/common",
    "test-deps": "lerna run test --scope={@gskTest/server,@gskTest/front}",
    "new-version": "lerna version --conventional-commits --yes",
    "diff": "lerna diff",
    "changed": "lerna changed --since",
    "list": "lerna list --since",
    "ci:build": "lerna run ci:build --since $_TAG"
  },
  "name": "yarn-workspaces",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "devDependencies": {
    "lerna": "^3.22.1"
  },
  "dependencies": {
    "@lerna/changed": "^4.0.0"
  }
}
