{
  "name": "@gskTest/common",
  "version": "1.1.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "test": "echo Common works like a charm!",
    "ci:build": "yarn install && yarn run build --if-present"
  }
}
