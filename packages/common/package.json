{
  "name": "@gskTest/common",
  "version": "1.9.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "test": "echo Common works like a charm!",
    "build": "echo this is the build from common",
    "ci:build": "yarn run build --if-present build",
    "zip": "cd dist && zip -r ../build-common.zip ."
  }
}
