{
  "name": "@gskTest/server",
  "version": "1.6.0",
  "main": "index.js",
  "license": "MIT",
  "dependencies": {
    "@gskTest/common": "^1.1.0"
  },
  "scripts": {
    "test": "echo Server works fine!",
    "ci:build": "yarn install && yarn run build --if-present"
  }
}
