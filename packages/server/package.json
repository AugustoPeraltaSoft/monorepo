{
  "name": "@gskTest/server",
  "version": "1.11.0",
  "main": "index.js",
  "license": "MIT",
  "dependencies": {
    "@gskTest/common": "^1.10.0"
  },
  "scripts": {
    "test": "echo Server works fine!",
    "build": "echo this is the build from server",
    "ci:build": "yarn run --if-present build",
    "zip": "cd dist && zip -r ../build-server.zip ."
  }
}
