{
  "name": "@gskTest/server",
  "version": "1.12.0",
  "main": "index.js",
  "license": "MIT",
  "dependencies": {
    "@gskTest/common": "^1.12.0"
  },
  "scripts": {
    "test": "echo Server works fine!",
    "build": "echo this is the build from server",
    "ci:build": "yarn run --if-present build",
    "zip": "node node_modules/.bin/npm-pack-all"
  }
}
